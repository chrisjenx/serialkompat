name: serialkompat
description: >-
  Backward/forward JSON wire-compatibility gate for kotlinx-serialization
  @Serializable models. Runs serialkompatCheck and posts a sticky PR comment.
author: chrisjenx
branding:
  icon: shield
  color: purple

inputs:
  ref:
    description: >-
      Baseline git ref to check against. Overrides the configured baselineRef
      (passed as -Pserialkompat.ref). Defaults to the plugin's baselineRef.
    required: false
    default: ""
  task:
    description: Gradle task to run.
    required: false
    default: serialkompatCheckAgainst
  report-path:
    description: Path to the JSON report the sticky comment is built from.
    required: false
    default: build/serialkompat/report.json
  gradle-args:
    description: Extra arguments passed to Gradle.
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Run serialkompat
      id: check
      shell: bash
      # Inputs are passed via env, NOT interpolated into the script body: GitHub sets these as
      # environment variables whose VALUES bash never re-parses, so a ref/task/args containing
      # shell metacharacters is treated as data, not code (no injection, no silent gate bypass).
      env:
        SK_REF: ${{ inputs.ref }}
        SK_TASK: ${{ inputs.task }}
        SK_GRADLE_ARGS: ${{ inputs.gradle-args }}
      run: |
        set +e
        REF_ARG=()
        if [ -n "$SK_REF" ]; then REF_ARG=(-Pserialkompat.ref="$SK_REF"); fi
        # SK_GRADLE_ARGS is intentionally word-split into multiple args; a variable's value is not
        # re-parsed for ';'/'|'/'$()', so this splits into args without executing anything.
        # shellcheck disable=SC2086
        ./gradlew "$SK_TASK" "${REF_ARG[@]}" $SK_GRADLE_ARGS --console=plain
        code=$?
        echo "exit_code=$code" >> "$GITHUB_OUTPUT"

    # Requires the CALLER workflow to grant `permissions: pull-requests: write` (or `issues: write`);
    # without it the GitHub API returns 403 and this step fails for a reason unrelated to compatibility.
    - name: Post sticky PR comment
      if: github.event_name == 'pull_request' && always()
      uses: actions/github-script@v9
      # report-path via env (read as process.env), not interpolated into the JS source — a value
      # containing a quote would otherwise inject arbitrary JavaScript into this token-privileged step.
      env:
        SK_REPORT_PATH: ${{ inputs.report-path }}
      with:
        script: |
          const fs = require('fs');
          const marker = '<!-- serialkompat -->';
          const path = process.env.SK_REPORT_PATH;
          let body;
          if (fs.existsSync(path)) {
            const report = JSON.parse(fs.readFileSync(path, 'utf8'));
            const s = report.summary || {};
            const icon = s.failed ? '❌' : (s.warning > 0 ? '⚠️' : '✅');
            const lines = (report.findings || [])
              .filter(f => !f.acknowledged)
              .map(f => `- **${f.severity}** \`${f.rule}\` — ${f.contract} (${f.direction})\n  ${f.message}`)
              .join('\n');
            body = `${marker}\n### ${icon} serialkompat\n\n`
              + `**${s.breaking ?? 0} breaking**, ${s.warning ?? 0} warning, ${s.acknowledged ?? 0} acknowledged.\n\n`
              + (lines || '_No incompatible changes._');
          } else {
            body = `${marker}\n### serialkompat\n\n_No report was produced (nothing configured to check)._`;
          }
          const { owner, repo } = context.repo;
          const issue_number = context.issue.number;
          const { data: comments } = await github.rest.issues.listComments({ owner, repo, issue_number });
          const existing = comments.find(c => c.body && c.body.includes(marker));
          if (existing) {
            await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body });
          } else {
            await github.rest.issues.createComment({ owner, repo, issue_number, body });
          }

    - name: Fail on incompatible changes
      if: steps.check.outputs.exit_code != '0'
      shell: bash
      run: |
        echo "serialkompat reported incompatible wire changes."
        exit 1
